# SecureGateway 用户手册

**版本 1.0 · 适用于 Windows 10 / Windows 11**

本手册面向公司员工，说明如何安装、登录和使用 SecureGateway 安全上网客户端。服务器搭建、账号权限等管理员操作不在本手册范围内，请参阅《USER_GUIDE》中的管理员章节。

---

## 目录

1. [SecureGateway 是什么](#1-securegateway-是什么)
2. [安装前准备](#2-安装前准备)
3. [安装](#3-安装)
4. [Windows 阻止程序运行时怎么办](#4-windows-阻止程序运行时怎么办)
5. [首次登录](#5-首次登录)
6. [忘记密码 / 重置密码](#6-忘记密码--重置密码)
7. [连接与断开](#7-连接与断开)
8. [界面说明](#8-界面说明)
9. [代理模式](#9-代理模式)
10. [系统托盘与开机自启](#10-系统托盘与开机自启)
11. [常见问题](#11-常见问题)
12. [安全须知](#12-安全须知)

---

## 1. SecureGateway 是什么

SecureGateway 是公司提供的 Windows 客户端。连接后，您电脑的网络流量会通过加密隧道经公司在海外的服务器转发，用于保护公司业务的上网安全。

您需要：

- 一台运行 Windows 10 或 Windows 11（64 位）的电脑；
- 管理员为您开通的 SecureGateway 账号（邮箱 + 密码）；
- 管理员发给您的安装文件 `SecureGateway-Setup-1.0.0.exe`（或免安装压缩包）。

无需另外安装任何软件，加密引擎已内置在程序中。

---

## 2. 安装前准备

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10 或 Windows 11，64 位 |
| 磁盘空间 | 约 250 MB |
| 网络 | 可以上网 |
| 账号 | 管理员已开通，并已授予 `gateway.access` 权限 |
| 权限 | 不需要电脑管理员权限（安装时可选择"仅为我安装"） |

> **重要：** 如果您的账号尚未被授予权限，登录时会提示"Access denied. Your account does not have gateway access."，请联系管理员开通，而不是反复尝试登录。

---

## 3. 安装

### 3.1 使用安装程序（推荐）

1. 双击 `SecureGateway-Setup-1.0.0.exe`。
2. 如果出现"用户账户控制"提示，点击**是**；如果您没有电脑管理员权限，安装程序会询问，请选择**仅为我安装（Install for me only）**。
3. 一路点击**下一步 / Next**，可勾选"创建桌面图标"。
4. 点击**安装 / Install**，完成后勾选"启动 SecureGateway"并点击**完成 / Finish**。

程序会出现在开始菜单中，名称为 **SecureGateway**。

### 3.2 使用免安装压缩包

1. 将压缩包 `SecureGateway-1.0.0-win-x64.zip` 解压到任意文件夹，例如 `D:\SecureGateway\`。
2. 解压后应看到 `SecureGateway.exe` 和一个 `v2ray-core` 文件夹。**两者必须放在同一目录中，不要单独移动 exe。**
3. 双击 `SecureGateway.exe` 启动。

### 3.3 升级到新版本

管理员发布新版本时，直接运行新的安装程序覆盖安装即可，您的服务器列表、登录状态和设置都会保留。升级前请先退出正在运行的 SecureGateway（右键托盘图标 → Exit）。

### 3.4 卸载

Windows 设置 → 应用 → 已安装的应用 → 找到 SecureGateway → 卸载。

---

## 4. Windows 阻止程序运行时怎么办

由于程序目前尚未进行商业代码签名，Windows 的安全机制可能会在首次运行时进行拦截。以下是三种常见情况及处理方法。

### 4.1 提示"Windows 已保护你的电脑"（SmartScreen）

这是最常见的情况，出现一个蓝色的提示窗口，标题为 **"Windows 已保护你的电脑"**，只显示一个"不运行"按钮。

处理方法：

1. 点击提示文字中的 **"更多信息"**。
2. 窗口会多出一个 **"仍要运行"** 按钮，点击它。
3. 之后不会再对同一文件重复提示。

### 4.2 提示"Smart App Control 已阻止此应用"（仅 Windows 11）

Windows 11 部分电脑（通常是 2022 年后出厂并全新安装系统的电脑）启用了 **Smart App Control（智能应用控制）**。它会阻止所有未经签名的程序，**并且不提供"仍要运行"选项**。

只有在管理员确认需要时，才按以下步骤关闭它：

1. 打开 **设置**（快捷键 `Win + I`）。
2. 左侧选择 **隐私和安全性**。
3. 点击 **Windows 安全中心**，再点击 **打开 Windows 安全中心**。
4. 在 Windows 安全中心左侧选择 **应用和浏览器控制**。
5. 点击 **Smart App Control 设置**。
6. 选择 **关闭**，在弹出的确认窗口中再次确认。
7. 重新运行 SecureGateway 安装程序。

请注意以下几点：

- **Smart App Control 一旦关闭，就无法再重新开启**，除非重装 Windows。这是 Windows 本身的限制。
- 如果该页面显示为"评估"模式，同样可以选择"关闭"。
- 如果选项是灰色不可选的，说明该电脑由公司 IT 策略统一管理，请联系 IT 部门处理。
- 关闭后，Windows Defender 和 SmartScreen 仍然正常工作，电脑并非没有防护。

如何判断是 SmartScreen 还是 Smart App Control？看提示窗口的文字：前者写着"Windows 已保护你的电脑"，且点击"更多信息"后有"仍要运行"；后者明确写着"Smart App Control"，且没有任何允许运行的按钮。

### 4.3 提示"引擎启动失败"或 v2ray-core 文件夹消失

个别杀毒软件会把加密引擎 `v2ray.exe` 误判为风险文件并隔离。表现为：程序可以打开，但点击 Connect 后日志显示 "v2ray-core not found"。

处理方法：

1. 打开 **Windows 安全中心 → 病毒和威胁防护 → 保护历史记录**，找到对被隔离文件的记录，选择 **允许 / 还原**。
2. 或者在 **病毒和威胁防护设置 → 排除项** 中，把 SecureGateway 的安装文件夹添加为排除项。
3. 若使用第三方杀毒软件，请在其隔离区中还原该文件并加入白名单。
4. 如果文件已被删除，重新运行安装程序即可恢复。

---

## 5. 首次登录

程序启动后首先显示登录窗口，未登录无法使用任何功能。

1. 在 **Email** 栏输入管理员为您开通的邮箱地址。
2. 在 **Password** 栏输入密码。点击输入框右侧的眼睛图标可以显示 / 隐藏密码，方便核对。
3. 如果这是您自己的电脑，可勾选 **Remember me（记住我）**，下次启动无需再次输入。**公用电脑请勿勾选。**
4. 点击 **Sign In（登录）**。

登录成功后会进入主界面。登录状态最长保持 **120 天**，之后需要重新登录；管理员也可以随时在服务器端注销您的登录。

**连续输错密码 10 次**，登录会被锁定 **5 分钟**，界面会显示剩余时间倒计时。锁定期间无法登录，请耐心等待，或通过"忘记密码"重置密码（重置成功会立即解除锁定）。

> **关于 Sign Up（注册）标签：** 员工账号统一由管理员创建，自行注册的账号没有使用权限，请勿使用该功能。

---

## 6. 忘记密码 / 重置密码

整个流程在程序内完成，不需要访问任何网站。

1. 在登录窗口的 **Email** 栏输入您的账号邮箱。
2. 点击 **Forgot password?（忘记密码？）**，窗口切换到重置界面。
3. 点击 **Send reset code（发送重置码）**。系统会向该邮箱发送一封邮件，一分钟内未收到请检查垃圾邮件文件夹。
4. 打开邮件。根据邮件模板，您会看到以下两种内容之一：
   - **一串数字重置码**：直接复制。
   - **一个链接**：可以右键复制链接地址；也可以直接点击它——它会打开一个无法显示的网页（地址以 `http://localhost:3000/#access_token=` 开头），这是正常现象，此时**复制浏览器地址栏中的完整地址**即可。
5. 回到程序，把复制的内容粘贴到 **Reset code, or paste the link from the e-mail** 输入框。
6. 在 **New password（新密码）** 栏输入新密码（至少 6 位）。
7. 点击 **Set new password（设置新密码）**。
8. 看到绿色提示"Password updated"后，用新密码正常登录。

重置码和链接的有效期约 **1 小时**，且只能使用一次。如果提示无效或过期，重新点击 **Send reset code** 获取新的即可。**重置链接等同于临时密码，请勿转发给任何人。**

---

## 7. 连接与断开

### 7.1 服务器从哪里来

登录后，程序会自动从公司服务器同步可用的线路，显示在 **Servers（服务器）** 标签页中，并带有 **SHARED（共享）** 标记。您通常不需要自己添加服务器。

如果列表为空或需要刷新，点击 **Sync Shared（同步共享服务器）**。

### 7.2 连接

1. 在 **Servers** 标签页选中一条线路（通常只有一条，已默认选中）。
2. 回到 **Dashboard（仪表盘）**，点击大按钮 **Connect（连接）**。
3. 几秒后状态变为绿色的 **Connected**，并显示服务器地址。
4. 正常上网即可。仪表盘上的 **Upload / Download** 会随流量增长，**Latency（延迟）** 显示到服务器的响应时间。

### 7.3 断开

点击 **Disconnect（断开）**。程序会自动恢复您电脑原来的网络设置。

### 7.4 验证是否生效

连接后用浏览器打开 `https://ifconfig.me`，如果显示的 IP 地址不是您所在网络的地址，而是公司服务器的地址，说明隧道已生效。

---

## 8. 界面说明

程序界面为英文，以下是各部分的含义。

### Dashboard（仪表盘）

| 显示项 | 含义 |
|--------|------|
| Connect / Disconnect | 连接 / 断开按钮 |
| Active Server | 当前使用的线路 |
| Upload / Download | 本次连接累计上传 / 下载流量 |
| Latency | 延迟（毫秒），显示 N/A 表示尚未测得或连接异常 |
| Duration | 本次连接持续时间 |
| Proxy Mode | 代理模式，见第 9 节 |
| SOCKS / HTTP | 本机代理端口，供需要手动配置代理的软件使用 |

### Servers（服务器）

| 按钮 | 作用 |
|------|------|
| Sync Shared | 从公司服务器重新同步线路列表 |
| + Add Server | 手动添加线路（一般不需要） |
| Import from Clipboard | 粘贴管理员给的 `vmess://` 或 `ss://` 链接导入线路 |
| Test / Test All | 测试选中线路 / 全部线路的延迟 |
| Edit / Duplicate / Delete | 编辑 / 复制 / 删除自己添加的线路（共享线路不可修改） |
| Share / Unshare | 管理员功能，普通用户无权限 |

### Settings（设置）

| 选项 | 说明 |
|------|------|
| Start with Windows | 开机自动启动（最小化到托盘） |
| Auto-connect on startup | 启动后自动连接上次使用的线路 |
| Minimize to system tray on close | 点击关闭按钮时最小化到托盘而不是退出 |
| Sign Out | 退出登录，返回登录窗口 |

### Log（日志）

显示程序和加密引擎的运行记录。遇到问题时，把这里的内容截图发给管理员，能大大加快排查。**Clear Log** 清空显示。

---

## 9. 代理模式

在 Dashboard 的 **Proxy Mode** 中选择：

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| Rule-Based（规则模式，默认） | 局域网和本地地址直连，其他流量走隧道 | 日常使用，推荐 |
| Global Proxy（全局模式） | 所有流量都走隧道 | 某些网站在规则模式下打不开时 |
| Direct (No Proxy)（直连） | 隧道保持连接，但系统不走代理 | 临时需要直连时，或手动给个别软件配置代理 |

切换模式立即生效，无需重新连接。

---

## 10. 系统托盘与开机自启

- 点击窗口右上角的关闭按钮，程序默认**最小化到系统托盘**（屏幕右下角的小图标），并**保持连接**，不会退出。
- **双击托盘图标**重新打开窗口。
- **右键托盘图标**可以快速 Connect / Disconnect，或选择 **Exit** 彻底退出程序。彻底退出时会自动断开连接并恢复网络设置。
- 在 Settings 中勾选 **Start with Windows** 和 **Auto-connect on startup**，之后每次开机会自动在后台连接，无需任何操作。

---

## 11. 常见问题

| 现象 | 原因与处理 |
|------|-----------|
| 双击安装程序没有反应，或弹出蓝色警告 | 见第 4 节 |
| 登录提示 "Invalid login credentials" | 邮箱或密码错误。注意大小写和多余空格；可点击眼睛图标核对密码；仍不行则用"忘记密码"重置 |
| 登录提示 "Access denied. Your account does not have gateway access." | 账号尚未被授权，联系管理员 |
| 登录提示 "Too many failed attempts" | 密码错误 10 次后锁定 5 分钟，等待倒计时结束，或直接重置密码 |
| 登录提示 "Connection error" | 电脑无法访问认证服务器。检查是否能正常上网；公司网络若有防火墙请联系 IT |
| Servers 列表为空 | 点击 Sync Shared；仍为空说明管理员尚未发布线路 |
| 点击 Connect 后显示 "V2Ray exited" 或 "did not open local port" | 打开 Log 标签页截图发给管理员。常见原因是杀毒软件隔离了引擎（见 4.3）或其他代理软件占用端口（程序会自动换端口，日志中有记录） |
| 已连接但网页打不开 | 先试试切换到 Global Proxy；查看 Latency 是否为 N/A（服务器不可达）；把 Log 发给管理员 |
| 已连接但 ifconfig.me 显示的还是本地 IP | 代理模式被设为 Direct，改回 Rule-Based |
| 断开后无法上网 | 极少见。打开 Windows 设置 → 网络和 Internet → 代理，关闭"使用代理服务器"，然后重启程序 |
| 每次开机都要重新登录 | 登录时勾选 Remember me；若仍然如此，可能是电脑上的杀毒软件清理了程序数据，请联系管理员 |
| 提示 "SecureGateway is already running" | 程序已在托盘中运行，双击托盘图标即可 |

---

## 12. 安全须知

- 账号密码仅供本人使用，不要与他人共用账号。
- 不要把重置密码的邮件、链接或重置码转发给任何人。
- 在公用电脑上不要勾选 Remember me，用完后点击 Settings → Sign Out。
- 本客户端仅用于保护公司业务上网，请遵守公司相关规定。
- 遇到任何异常，把 **Log** 标签页的内容截图发给管理员，不要自行修改系统网络设置。

---

*SecureGateway 用户手册 · 如有疑问请联系公司 IT 管理员*
