<Window x:Class="SecureGateway.UI.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="SecureGateway - Sign In" Height="660" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#1E1E2E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,24,0,0">
            <!-- Shield icon area -->
            <Border Width="64" Height="64" CornerRadius="32" Background="#313244"
                    HorizontalAlignment="Center" Margin="0,0,0,12">
                <TextBlock Text="&#x1F6E1;" FontSize="30" HorizontalAlignment="Center"
                           VerticalAlignment="Center" Foreground="#89B4FA"/>
            </Border>
            <TextBlock Text="SecureGateway" FontSize="24" FontWeight="Bold"
                       Foreground="#CDD6F4" HorizontalAlignment="Center"/>
            <TextBlock Text="Enterprise VPN Client" FontSize="13"
                       Foreground="#A6ADC8" HorizontalAlignment="Center" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Login Form -->
        <Border Grid.Row="1" Margin="32,16,32,20" Padding="24"
                Background="#313244" CornerRadius="12">
            <StackPanel>
                <!-- Tab: Sign In / Sign Up -->
                <Grid Margin="0,0,0,18">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnSignInTab" Grid.Column="0" Content="Sign In"
                            Background="#89B4FA" Foreground="#1E1E2E" FontWeight="SemiBold"
                            FontSize="13" Height="36" BorderThickness="0" Cursor="Hand"
                            Click="OnSignInTabClick">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                        CornerRadius="6,0,0,6" Padding="12,6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="BtnSignUpTab" Grid.Column="1" Content="Sign Up"
                            Background="#45475A" Foreground="#A6ADC8"
                            FontSize="13" Height="36" BorderThickness="0" Cursor="Hand"
                            Click="OnSignUpTabClick">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                        CornerRadius="0,6,6,0" Padding="12,6">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </Grid>

                <!-- Email -->
                <TextBlock Text="Email" Foreground="#CDD6F4" FontSize="13" Margin="0,0,0,6"/>
                <TextBox x:Name="TxtEmail" Height="38" FontSize="14"
                         Background="#1E1E2E" Foreground="#CDD6F4" BorderBrush="#45475A"
                         Padding="10,8" CaretBrush="#CDD6F4"
                         KeyDown="OnInputKeyDown"/>

                <!-- Password -->
                <TextBlock Text="Password" Foreground="#CDD6F4" FontSize="13" Margin="0,12,0,6"/>
                <Grid Height="38">
                    <PasswordBox x:Name="TxtPassword" FontSize="14"
                                 Background="#1E1E2E" Foreground="#CDD6F4" BorderBrush="#45475A"
                                 Padding="10,8" CaretBrush="#CDD6F4"
                                 KeyDown="OnInputKeyDown"/>
                    <TextBox x:Name="TxtPasswordVisible" FontSize="14"
                             Background="#1E1E2E" Foreground="#CDD6F4" BorderBrush="#45475A"
                             Padding="10,8" CaretBrush="#CDD6F4"
                             Visibility="Collapsed"
                             KeyDown="OnInputKeyDown"/>
                    <Button x:Name="BtnTogglePassword" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Width="36" Height="30"
                            Margin="0,0,4,0" Cursor="Hand" Background="Transparent"
                            BorderThickness="0" Click="OnTogglePasswordClick">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock x:Name="IconTogglePassword" Text="&#xE7B3;"
                                   FontFamily="Segoe MDL2 Assets" FontSize="14"
                                   Foreground="#6C7086" VerticalAlignment="Center"/>
                    </Button>
                </Grid>

                <!-- Forgot Password (sign in mode only) -->
                <TextBlock x:Name="LnkForgotPassword" Text="Forgot password?"
                           Foreground="#89B4FA" FontSize="12" Margin="0,6,0,0"
                           HorizontalAlignment="Right" Cursor="Hand"
                           MouseLeftButtonUp="OnForgotPasswordClick">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="TextDecorations" Value="Underline"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Confirm Password (for sign up) -->
                <TextBlock x:Name="LblConfirmPassword" Text="Confirm Password" Foreground="#CDD6F4"
                           FontSize="13" Margin="0,12,0,6" Visibility="Collapsed"/>
                <Grid x:Name="GridConfirmPassword" Height="38" Visibility="Collapsed">
                    <PasswordBox x:Name="TxtConfirmPassword" FontSize="14"
                                 Background="#1E1E2E" Foreground="#CDD6F4" BorderBrush="#45475A"
                                 Padding="10,8" CaretBrush="#CDD6F4"
                                 KeyDown="OnInputKeyDown"/>
                    <TextBox x:Name="TxtConfirmPasswordVisible" FontSize="14"
                             Background="#1E1E2E" Foreground="#CDD6F4" BorderBrush="#45475A"
                             Padding="10,8" CaretBrush="#CDD6F4"
                             Visibility="Collapsed"
                             KeyDown="OnInputKeyDown"/>
                    <Button x:Name="BtnToggleConfirmPassword" HorizontalAlignment="Right"
                            VerticalAlignment="Center" Width="36" Height="30"
                            Margin="0,0,4,0" Cursor="Hand" Background="Transparent"
                            BorderThickness="0" Click="OnToggleConfirmPasswordClick">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                        <TextBlock x:Name="IconToggleConfirmPassword" Text="&#xE7B3;"
                                   FontFamily="Segoe MDL2 Assets" FontSize="14"
                                   Foreground="#6C7086" VerticalAlignment="Center"/>
                    </Button>
                </Grid>

                <!-- Error/Status message -->
                <TextBlock x:Name="TxtStatus" Text="" TextWrapping="Wrap"
                           Foreground="#F38BA8" FontSize="12" Margin="0,10,0,0"/>

                <!-- Lockout / attempt counter bar -->
                <TextBlock x:Name="TxtLockoutBar" Text="" TextWrapping="Wrap"
                           Foreground="#FAB387" FontSize="12" HorizontalAlignment="Center"
                           Margin="0,6,0,0" Visibility="Collapsed"/>

                <!-- Remember me -->
                <CheckBox x:Name="ChkRememberMe" Content="Remember me"
                          Foreground="#A6ADC8" FontSize="12" Margin="0,12,0,0"
                          VerticalContentAlignment="Center" Cursor="Hand"/>

                <!-- Submit Button -->
                <Button x:Name="BtnSubmit" Content="Sign In" Height="42" FontSize="15"
                        FontWeight="SemiBold" Margin="0,14,0,0" Cursor="Hand"
                        Background="#89B4FA" Foreground="#1E1E2E" BorderThickness="0"
                        Click="OnSubmitClick">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    CornerRadius="8" Padding="12,8">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#74A8F7"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.5"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Loading indicator -->
                <TextBlock x:Name="TxtLoading" Text="Authenticating..."
                           Foreground="#A6ADC8" FontSize="12" HorizontalAlignment="Center"
                           Margin="0,8,0,0" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
